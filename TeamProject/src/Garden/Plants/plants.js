import React, { Component } from 'react';
import {
  Text, View, StyleSheet, Image, 
} from 'react-native';

/* Plant component gives a search bar to add the plants
 * you wish to grow by click add button.
 */

export default class Plant extends Component {
  render() {
    return(
      <View>
        <Text > "1"</Text>
      </View>
    );
  }

}